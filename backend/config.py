# Application Configuration

# Secret key for session management and security (change this to a strong, unique key)
SECRET_KEY = 'your_secret_key'

# Debug mode (set to True during development, and False in production)
DEBUG = True

# Database Configuration

# MongoDB URI (replace with your actual MongoDB connection string)
MONGO_URI = 'mongodb://localhost/your_database_name'

# Additional Configuration Options

# Other configuration settings can be added here as needed
